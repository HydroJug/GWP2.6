query DeliveryInput {
  cart {
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
    deliveryGroups {
      id
      deliveryOptions {
        handle
        title
        cost {
          amount
        }
      }
    }
    buyerIdentity {
      customer {
        id
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "combined_discount", key: "config") {
      value
    }
  }
}
