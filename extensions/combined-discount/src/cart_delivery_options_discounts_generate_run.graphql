query DeliveryInput($eligibilityTags: [String!]) {
  cart {
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
    deliveryGroups {
      id
      deliveryOptions {
        handle
        title
        cost {
          amount
        }
      }
    }
    buyerIdentity {
      customer {
        id
        hasAnyTag(tags: $eligibilityTags)
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "combined_discount", key: "config") {
      value
    }
  }
}
