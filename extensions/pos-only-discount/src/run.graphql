query CartInput {
  cart {
    lines {
      id
      quantity
    }
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
    attribute(key: "channel") {
      value
    }
  }
  discount {
    metafield(namespace: "pos_only", key: "config") {
      value
    }
  }
}
